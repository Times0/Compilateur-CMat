Square root (Newton Raphson)
=========
void main() {
    float x = 2.0;
    float epsilon = 0.001;
    float guess = 1.0;
    float abs = 1.0;

    while (abs >= epsilon) {
        guess = (x / guess + guess) / 2.0;
        abs = guess * guess - x;
        if (abs < 0) abs = -abs;
    }

    print(guess);
}
=========
1.4142157